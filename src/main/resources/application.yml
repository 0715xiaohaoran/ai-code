spring:
  application:
      name: ai-code

  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ai_code_study
    username: root
    password: root

server:
  port: 8123
  servlet:
    context-path: /api
springdoc:
  groupp-configs:
    - group: "default"
      packages-to-scan: com.xhr.aicode.controller

knife4j:
  enable: true
  setting:
    language: zh-cn



